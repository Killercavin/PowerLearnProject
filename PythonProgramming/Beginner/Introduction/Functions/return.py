# Even though this says it returns a string,
#   it will actually return an integer!
def area(length:int, width:int) -> str:
    return length * width

# Try running it yourself!
print(area(5, 3)) # Multiple return types